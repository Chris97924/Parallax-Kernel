# Parallax runtime configuration
# Copy this file to `.env` and edit values for your local setup.
# All three are optional; defaults resolve under the project root.

# Path to the SQLite database file.
PARALLAX_DB_PATH=./db/parallax.db

# Path to the Obsidian vault root (markdown storage).
PARALLAX_VAULT_PATH=./vault

# Path to the canonical schema (DDL) file.
PARALLAX_SCHEMA_PATH=./schema.sql

# --- M8 semantic retrieval (docs/m8-prep/m8-semantic-retrieval-plan.md) ---
# Hybrid lexical + dense (RRF) retrieval. Default OFF — when unset/false the
# retrieval path is byte-for-bit identical to the pre-M8 lexical behavior.
# PARALLAX_SEMANTIC_RETRIEVAL=true
# Ollama base URL for embeddings. When set, the live bge-m3 provider is used;
# when unset, an offline deterministic stub provider is used (no network).
# PARALLAX_EMBEDDING_BASE_URL=http://192.168.1.134:11434
# Embedding model name (default: bge-m3, 1024-dim).
# PARALLAX_EMBEDDING_MODEL=bge-m3

# Apex M5 audit-row SQLite path (per docs/m5-prep/audit-db-path-config.md).
# REQUIRED: parallax-server refuses to start if unset or empty (spec §3 —
# no operator-specific default is embedded in production code).
# Path MUST be absolute, parent directory MUST exist and be writable.
# Suggested locations (set explicitly for your environment):
#   Windows dev    : E:\Parallax\data\audit.db
#   Linux ZenBook  : /home/chris/parallax-kernel/db/audit.db
PARALLAX_AUDIT_DB_PATH=
